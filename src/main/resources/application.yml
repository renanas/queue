spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

    listener:
      simple:
        default-requeue-rejected: false

  datasource:
    url: jdbc:postgresql://localhost:5432/queue
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true